node_name: "embedding_node"
environment: "dev"

next_nodes:
  - "http://localhost:8015"  # entity_extraction_node

max_retries: 3
backoff_base: 2.0
max_queue_depth: 500
max_workers: 2
task_timeout: 300
shutdown_timeout: 60
