feat: auth overhaul — closed registration, role rename, invitations, permissions matrix, cross-org views

Phase 1 — Lockdown
- registration_open defaults to false; only bootstrap user (zero users in DB) can self-register
- /auth/register returns 403 for non-first users unless admin explicitly reopens registration
- is_registration_open() safe-defaults to false even when SystemConfig row is missing
- Login page auto-hides Register link via providers.registration_open

Phase 4 — Role model cleanup
- Rename manager -> org_admin; add viewer role (admin/org_admin/user/viewer)
- normalize_role() translates legacy manager -> org_admin for back-compat with stale JWTs
- require_org_admin dep + require_viewer dep added; require_manager kept as back-compat alias
- org_admin now bypasses BucketPermission checks identically to admin
- Idempotent init_db migration: UPDATE user/userorganization SET role=org_admin WHERE role=manager
- All role validator tuples across admin.py, users.py, archive.py, setup.py, collaboration.py updated
- Frontend: role pickers, guards, Badge colours updated; manager label removed

Phase 2 — Admin user CRUD UI
- New endpoint POST /api/admin/users/{id}/reset-password (clears lockout, hashes new pw)
- admin/Users.tsx redesigned: Create user modal, Reset password, Deactivate/Reactivate row actions
- Role picker shows description hint on each option

Phase 3 — Invitation tokens
- New Invitation SQLModel (token, org, role, bucket_grants JSON, invited_email, expires_at, redeemed_at)
- New router routers/invitations.py with 5 endpoints (admin create/list/revoke + public lookup/redeem)
- Redeem grants prebound bucket permissions and auto-creates viewer org memberships for cross-org buckets
- Frontend: Invitations tab in admin/Users.tsx with copy-link UX; public /invite/:token redeem page

Phase 5 — Permissions matrix
- GET  /api/admin/users/{id}/access-matrix: every org x bucket with membership role + permissions
- POST /api/admin/users/{id}/access-matrix: atomic bulk update; auto-adds viewer org row on bucket grant
- New AccessMatrixEditor.tsx full-screen modal with org panels, membership and per-bucket dropdowns
- Diff-tracking: only changed rows sent to the backend

Phase 6 — Cross-org views
- GET /api/buckets?across_orgs=true: all orgs the user is member of + cross-org bucket grants
- GET /api/documents?across_orgs=true: expands org filter via UserOrganization; org_name in payload
- UserBuckets and Documents pages: Current org / All my orgs toggle (only shown with 2+ memberships)

Tests: 539 passed, 1 skipped
